%tr
  %td= serial.phone
  %td= User.find(serial.user_id).name if serial.user_id
  %td= link_to "下载", "/img/qrcode/#{serial.id}.png", :download => serial.product.product_name + serial.id.to_s
  %td= serial.serial_no
  %td= serial.state
